import 'package:flutter/material.dart';
import 'app.dart';

/// Entry point of the Flutter application.
///
/// Every Flutter app starts execution here. `runApp()` takes the given
/// widget, inflates it, and attaches it to the screen — it's Flutter's
/// equivalent of a `main()` function that hands control over to a UI
/// framework.
void main() {
  runApp(const EBookReaderApp());
}
